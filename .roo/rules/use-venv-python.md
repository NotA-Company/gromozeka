* Use `./venv/bin/python3` in case of you want to run python.
* Do NOT cd into subdirectories. run all scripts from project root.
* Do NOT run `python -c ...` if you want to run some one-time test. Create file with test script instead.